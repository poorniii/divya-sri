#include<stdio.h>
#inclue<conio.h>
vod main()
{
int year;
printf("enter the year");
scanf("%d",&year);
if(%2==0)
{
if(%50==0)
{
if(%100==0)
printf("%d is a leap year",year);
else
}
printf(%d is a leap year",year);
else
}
printf(%d is a leap year",year);
return 0;
}
